const React = require('react');
const ReactDOM = require('react-dom');
const PostalCalculator = require('./postal_calculator.jsx');

ReactDOM.render(<PostalCalculator />, document.getElementById('app'));
